# GitDemo

Project created as part of the Git Hands-On Lab exercises.

## Contents
- `welcome.txt` – sample file from Lab 1 (init/add/commit)
- `.gitignore` – ignores `*.log`, `log/`, and `*.orig` backup files (Lab 2 & 4)
- `hello.xml` – sample file used for branching/merge-conflict practice (Lab 3 & 4)
